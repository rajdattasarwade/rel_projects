import { RestrictedFormInputDirective } from './restricted-form-input.directive';

describe('RestrictedFormInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictedFormInputDirective();
    expect(directive).toBeTruthy();
  });
});
